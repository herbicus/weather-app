describe('weatherFactory', function() {

  beforeEach(module('weatherModule'));

  it('should ...', inject(function(weatherFactory) {

    //expect(weatherFactory.doSomething()).toEqual('something');

  }));

});
